{
  "name": "mini-shed",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "lerna run watch --parallel",
    "build": "lerna run build"
  }
}